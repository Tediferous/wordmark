version: "3"

tasks:
  templ:
    desc: Run templ with integrated server and hot reload
    cmds:
      - templ generate --watch --proxy="http://localhost:1717" --cmd="go run ./main.go" --open-browser=true

  dev:
    desc: Start development server with hot reload
    cmds:
      - task --parallel templ
